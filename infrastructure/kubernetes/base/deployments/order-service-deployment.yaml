# Order Service Deployment with Migration Init Container
# Implements automatic database migrations before application startup

apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: lugx-dev
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/component: microservice
    app.kubernetes.io/part-of: lugx-gaming
    app.kubernetes.io/version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: order-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: order-service
        app.kubernetes.io/component: microservice
    spec:
      serviceAccountName: order-service-sa
      
      # Init container for database migrations
      initContainers:
      - name: migration
        image: order-service:latest
        imagePullPolicy: IfNotPresent
        command: ["alembic", "upgrade", "head"]
        env:
        - name: DATABASE_URL
          value: "postgresql+asyncpg://$(ORDER_DB_USER):$(ORDER_DB_PASSWORD)@$(ORDER_DB_HOST):$(ORDER_DB_PORT)/$(ORDER_DB_NAME)"
        - name: ORDER_DB_HOST
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: order-db-host
        - name: ORDER_DB_PORT
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: order-db-port
        - name: ORDER_DB_NAME
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: order-db-name
        - name: ORDER_DB_USER
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: order-db-user
        - name: ORDER_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: order-db-password
        - name: ENVIRONMENT
          value: "development"
        resources:
          limits:
            memory: "256Mi"
            cpu: "200m"
          requests:
            memory: "128Mi"
            cpu: "100m"
      
      # Main application container
      containers:
      - name: order-service
        image: order-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8002
          name: http
          protocol: TCP
        env:
        - name: DATABASE_URL
          value: "postgresql+asyncpg://$(ORDER_DB_USER):$(ORDER_DB_PASSWORD)@$(ORDER_DB_HOST):$(ORDER_DB_PORT)/$(ORDER_DB_NAME)"
        - name: ORDER_DB_HOST
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: order-db-host
        - name: ORDER_DB_PORT
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: order-db-port
        - name: ORDER_DB_NAME
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: order-db-name
        - name: ORDER_DB_USER
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: order-db-user
        - name: ORDER_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: order-db-password
        - name: REDIS_HOST
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: redis-host
        - name: REDIS_PORT
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: redis-port
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: redis-password
        - name: ENVIRONMENT
          value: "development"
        - name: LOG_LEVEL
          value: "INFO"
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sh", "-c", "sleep 10"]
      
      # Graceful shutdown
      terminationGracePeriodSeconds: 30
      
      # Host networking to access external databases
      hostNetwork: false
      dnsPolicy: ClusterFirst

---
# Order Service for Development
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: lugx-dev
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/component: microservice
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: order-service